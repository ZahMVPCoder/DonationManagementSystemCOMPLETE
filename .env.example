# ============================================================================
# BACKEND ENVIRONMENT VARIABLES - Donation Management System
# ============================================================================
# IMPORTANT: This is a template file. Copy this to .env and fill in with 
# your actual values. The .env file should NEVER be committed to version control.
# ============================================================================

# Database Configuration
# PostgreSQL connection string from Neon (https://console.neon.tech)
# Format: postgresql://user:password@host/database?sslmode=require
# Get your connection string from: https://console.neon.tech
DATABASE_URL=postgresql://user:password@ep-example.us-east-1.aws.neon.tech/dbname?sslmode=require

# Authentication & Security
# Generate a strong random secret key for JWT signing in production
# Security tip: Use at least 32 random characters
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-here

# Server Configuration
# Port for Express server to listen on (default: 5000)
PORT=5000

# Environment
# Values: development, test, production
NODE_ENV=development

# Frontend URL (for CORS)
# Allowed origin for frontend requests
# Development: http://localhost:5173
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:5173

# ============================================================================
# Optional Configuration
# ============================================================================
# Uncomment and configure if you need these features

# Email Configuration (for future notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@donormanagement.com

# JWT Expiration
# JWT_EXPIRY=7d
